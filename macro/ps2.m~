data = csvread('/Users/garidor/Desktop/first-year/macro/data.csv', 1);

corrcoef(data(:,2), data(:, 3))
corrcoef(sim_y_series, sim_wage_2)

function c
y_growth = zeros(1, 199);
for i = 2:(length(y_growth)+1)
    y_growth(i-1) = sim_y_series(i) / sim_y_series(i-1);
end
acf = autocorr(y_growth, 1)